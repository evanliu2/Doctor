<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- This is the styling for action bar -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="android:textColorHint">@color/darkgreen</item>
    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="background">@color/darkestgray</item>
        <item name="titleTextStyle">@style/MyActionBarTitle</item>

    </style>

    <style name="MyActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/lightgreen</item>
    </style>

    <style name = "AlertDialog" parent="@android:style/Theme.Dialog">
        <item name = "android:textColor">#8fff3a</item>
        <item name = "android:background">#27292d</item>
        <item name = "android:textSize">14sp</item>
    </style>

</resources>
